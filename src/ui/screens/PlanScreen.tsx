/** Plan screen placeholder (WR-002). Real distance/loop/surface inputs land in WR-008. */
export function PlanScreen() {
  return (
    <section className="wr-screen">
      <h1>Plan a ride</h1>
      <p className="wr-muted">
        Tell WindRide your distance and preferences and it generates routes shaped by today’s wind,
        shelter and daylight.
      </p>
      <p className="wr-muted">The ride inputs and generate flow arrive in WR-008.</p>
    </section>
  );
}
