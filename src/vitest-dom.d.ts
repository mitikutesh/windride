// Loads @testing-library/jest-dom's matcher type augmentations (toBeInTheDocument,
// toHaveAttribute, ...) into the TypeScript program for vitest assertions (WR-002).
import '@testing-library/jest-dom/vitest';
