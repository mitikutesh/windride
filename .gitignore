# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
dev-dist/
*.local
*.tsbuildinfo

# AWS CDK synth output (infra/)
cdk.out/

# Env / secrets (see .env.example for required keys)
.env
.env.*
!.env.example
!.env.test

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Test / coverage
coverage/

# Generated acceptance report (npm run accept)
accept-report.md

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Vite cache
.vite/

# Python (tools/)
.venv/
__pycache__/
.pytest_cache/
*.pyc
tools/exposure_grid/.cache/

# Strava local OAuth config (client secret + refresh token)
tools/.strava.json

# Hand-downloaded Bikeland GPX inputs + the cached Overpass reply (WR-052). The BUILT catalog
# (public/data/curated-fi.json) is committed; its raw inputs are not ours to redistribute.
tools/curated_in/
