{
  "name": "windride-infra",
  "version": "0.1.0",
  "private": true,
  "description": "WindRide AWS infrastructure (AWS CDK, TypeScript). Isolated from the app toolchain (WR-037+).",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "vitest run && cdk deploy"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.1000.0",
    "aws-cdk-lib": "^2.261.0",
    "constructs": "^10.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  }
}
