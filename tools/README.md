# tools/

Offline helpers that never ship in the app bundle:
- exposure_grid/ — created by WR-018 (Python: OSM extract -> exposure JSON)
- strava-auth.mjs — created by WR-023 (one-time localhost OAuth for the single-player app)
